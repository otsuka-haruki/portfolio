import { useState } from 'react';
import { Button, Typography, Box, CircularProgress } from '@mui/material';

const Index = () => {
  const callAction = async () => {
  }

  return (
    <>
      <Button variant="outlined" onClick={callAction} >Action</Button>
    </>
  )
}

export default Index;